// ConsoleApplication1.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include<algorithm>
#include<vector>
#include<iostream>
using namespace std;

class Solution {
public:
	vector<vector<int>> fourSum(vector<int>& nums, int target) {

	}
};

int _tmain(int argc, _TCHAR* argv[])
{
	vector<int> a;
	vector<vector<int>> b;
	Solution ret;
	a = vector<int>{1, 0, -1, 0, -2, 2};
	b = ret.fourSum(a, 6);
	return 0;
}

